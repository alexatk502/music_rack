[package]
name = "rack-graph"
version.workspace = true
edition.workspace = true

[dependencies]
rack-core = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1"
